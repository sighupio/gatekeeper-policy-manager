.vscode
# `go build` with no -o drops the binary here, named after the module
gatekeeper-policy-manager
static-content/*
tests/e2e/test-results
node_modules
web-client/build
**/mise.local.toml
# rendered by `mise run render` for the deprecated-API check
gpm.yaml
rendered_chart.yaml
# Local working notes for the drop-React migration; not part of the product.
/PORTING.md
/PORTING.backup-*.md
/docs/dev
